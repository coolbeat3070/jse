package oop01.syntax;
/*
상수 선언 http://marvell.tistory.com/16

상수는 프로그램이 실행되는 동안 (선언과 동시에 값을 설정했다면) 값이 변하지 않는 것.

(선언과 동시에 값을 설정 안했다면) 값을 한 번만 설정이 가능한 것.

명령어로는 c++에서는 const로 Java에서는 final이 있다.

상수는 literal 상수와 symbolic 상수로 나뉘어 진다.

* */
/*
C언어에서는 상수를 종류에 따라 다음과 같이 정의함
1. 리터럴(literal) 상수는 이름이 없는 상수
2. 심볼릭(symbolic) 상수는 이름이 있는 상수
	const 키워드를 사용해서 선언
	자바는 final 키워드를 사용해서 선언한다는 점만 다를 뿐이다.
 * */
public class No07_ConstantDemo {
	static final float PI = 3.14f;
	/*심볼릭 상수의 관례
	 1. 명사
	 2. 대문자
	 3. 여러 단어의 조합이면 COMPANY_ADDRESS 이런 식으로..
	 * */
	
	
}
